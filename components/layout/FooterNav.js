import Card from "../UI/Card";

import Styles from "./FooterNav.module.css";

const FooterNav = (event) => {
  return (
    <div className={Styles["main-container"]}>
      <Card></Card>
    </div>
  );
};

export default FooterNav;
